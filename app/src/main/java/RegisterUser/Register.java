package RegisterUser;

import User.Users;

public class Register extends Users {
    public Register(String name, String email, String phoneNumber, String password, String confirmPassword) {
        super(name, email, phoneNumber, password, confirmPassword);
    }
}
